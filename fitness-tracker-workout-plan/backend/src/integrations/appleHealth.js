/**
 * Stub: Apple Health integrations are typically via iOS apps / HealthKit,
 * then synced to your backend. This is a placeholder.
 */
export async function fetchAppleHealthMetrics({ since }) {
  return { steps: 0, calories: 0, heartRateAvg: 0, since };
}
